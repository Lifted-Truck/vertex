# Trace — kit 2.4.1 retrofit (2026-08-18)

**What changed.**
- `.github/workflows/ci.yml` — created from `kit/templates/ci.github.yml`
  (2.0.0 baseline gap). Runs `./verify fast` only; `full` stays out of CI
  because it names the human-run Layer-E audit (Q-004). **No `pip install
  pyyaml`** — deliberate, see below.
- `CLAUDE.md` — appended marker-delimited `## Mailbox` (kit 2.1.0 retrofit
  action), using the 2026-08-18 corrected wording: other repos' exchanges may
  be READ freely, never ACTED on or raised as ours. No prior section existed.
- `.claude/skills/vertex/scripts/layer0_check.py` — **protected path, human-
  gated and approved this session.** Two fallback defects fixed (D-006).
- `project.manifest.json` — `"kit_version": "2.4.1"`.
- `DECISIONS.md` — D-006, D-007.

**Why.** Autonomous resident's 2026-08-18 request to bring the repo to current
kit. `currency.py` opened at `pre-2.0.0 / BEHIND by 5 entries`; 2.2.0/2.3.0/
2.4.0 were already `[x]` from the hand migration to vendored gates earlier the
same day, leaving CI + the Mailbox charter section + the declaration.

**The finding (this is the part worth reading).** Testing CI parity *before*
writing the workflow surfaced two real defects in the Layer-0 regex fallback —
the path used by any environment without PyYAML, i.e. every bare CI runner:

- **Bug A (false negative).** `^profile\s*:\s*(concept|system)` is quote-blind,
  but the schema writes `profile: "concept"`. The fallback rejected the good
  fixture. CI would have been red on its first run.
- **Bug B (false positive, the serious one).** The source check counted
  `\bsource\s*:\s*\S+`, and `""` satisfies `\S+`. The cardinal
  anti-confabulation rule — every `key_concept` carries a non-empty `source` —
  was **silently unenforced** on the fallback path.

They interact: fixing A alone would have exposed B, converting a loudly broken
gate into a quietly blind one. Verified before the fix — a valid-except-empty-
source map failed the fallback reporting *only* `profile must be concept|system`,
never the empty source.

**Evidence.**
- Before fix, no PyYAML: good fixture exit 1 (`profile must be concept|system`);
  empty-source map exit 1 for the wrong reason.
- After fix, no PyYAML: good fixture exit 0, bad fixture exit 1, empty-source
  map exit 1 with `key_concepts: 1 terms but only 0 non-empty sources`.
- With PyYAML: unchanged (good 0 / bad 1).
- Whole oracle both paths: `verify[fast]: green`.

**Step-4b proofs (re-run; they come apart, so all three).**
1. Green, and project gates still red-capable: planting `source: ""` in the
   good fixture → `layer0 REJECTED the good fixture`, exit 1; restored → green.
2. Sourced: `grep -c 'kit/kit-gates.sh' verify` → 4.
3. Fires: planted `audit-plant.md` → gate named the file; removed → green.

**Currency.** Open: `kit: 2.4.1  declared: pre-2.0.0  BEHIND by 5 entries`.
Close: `kit: 2.4.1  declared: 2.4.1  CURRENT — nothing to do`.

**Verify:** `./verify fast` green (both the PyYAML and the fallback path).
**Git hash:** see the commit carrying this trace. **Pushed:** no — the human's.
