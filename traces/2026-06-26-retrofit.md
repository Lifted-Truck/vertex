# retrofit — catch the Vertex skill repo up to ecosystem scaffolding

- **Queue item:** unqueued (this retrofit bootstraps the queue; it is DECISIONS D-001)
- **Why:** the repo shipped a real v1 skill, a pending revision brief, and two
  generated maps, but had zero harness — no charter, oracle, ROADMAP, or record.
  Applied ONBOARDING.md Part 2 non-destructively so future sessions have a
  single source of truth and a gate.
- **Evidence consulted:**
  - `~/Documents/Claude/autonomous/ONBOARDING.md` §Part 2 (retrofit procedure).
  - Harness templates: `autonomous/harness/{CLAUDE.md,README.md,ROADMAP.md,verify,.claude/**}`.
  - Reference impls: `distillery/` (manifest shape, verify structure), `dispatch/verify`.
  - Repo gap survey: `git ls-files`, `find`, `.gitignore`, `VERTEX_Skill_Scaffolding.md`,
    `VERTEX_Skill_Trinary_Update.md`, current `SKILL.md` (still v1 binary).
- **What changed (all create except the last):**
  - Charter/direction/record: `CLAUDE.md` (invariant layer + §Domain + KL block),
    `ROADMAP.md` (absorbs Trinary brief as Q-001, scaffolding §12 + parsing bug as
    Q-002..Q-005), `DECISIONS.md` (D-001..D-005 + inherited DI-A/DI-B), `README.md`.
  - Oracle: `verify` (wraps structure + manifest + SKILL frontmatter + script
    syntax + a layer0 good/bad self-test), `tests/fixtures/{good,bad}_map.md`.
  - Manifest: `project.manifest.json` (9-survey answers, marked retrofit).
  - Knowledge loop: `INDEX.md`, `LIBRARY.md` (seeded L0001 — the real stage-body
    parser bug from the skill's first build).
  - Invariant `.claude/` layer copied verbatim: `settings.json`, 3 hooks, 3 agents,
    provenance skill. `traces/README.md` + this trace.
  - **Modified (append-only):** `.gitignore` — added `.harness/` between
    `HARNESS-RETROFIT` markers; existing rules untouched and still win.
- **Not touched (deliberately):** `.claude/skills/vertex/**` (the product),
  `VERTEX_Skill_Scaffolding.md`, `VERTEX_Skill_Trinary_Update.md` (source docs,
  pointed at from ROADMAP), `vertex/*.md` (gitignored run artifacts).
- **Alternatives rejected:** implementing the Trinary revision as part of the
  retrofit — rejected: retrofit adds scaffolding only, and the brief carries
  unresolved §5 questions the human owns (absorbed as Q-001 instead).
- **Red-suite honesty:** no pre-existing test suite; the `layer0_check.py`
  validator was already green. Nothing quarantined.
- **Verify:** `./verify full` → exit 0, git 848dda0
  (`{"target":"full","exit":0,"git":"848dda0"}`). `fast` green: structure,
  manifest, SKILL frontmatter, script syntax, and the layer0 self-test (good
  fixture passes, bad fixture fails) all pass.
- **Open questions:** the four Trinary §5 questions (persisted-map migration,
  composite persistence, real system tooling, overwrite-vs-timestamp) — carried
  in ROADMAP Q-001/Q-002/Q-005 and DECISIONS D-004, for the human. No briefs
  filed (Vertex is self-contained, not an ecosystem consumer/provider).
